using UnityEngine;

namespace CustomAssets.EditorTools
{
    /// <summary>
    /// Attribute that renders a Unity Layer selector for integer (layer index) and string (layer name) fields.
    /// </summary>
    public class LayerSelectorAttribute : PropertyAttribute
    {
    }
}

